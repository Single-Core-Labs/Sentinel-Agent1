--- a/rust/platform/platform.bzl
+++ b/rust/platform/platform.bzl
@@ -28,6 +28,10 @@ _RUSt_TOOLCHAIN_TRIPLES = {
     "x86_64-pc-windows-msvc": "x86_64-pc-windows-msvc",
     "x86_64-pc-windows-gnu":  "x86_64-pc-windows-gnu",
     "aarch64-pc-windows-msvc": "aarch64-pc-windows-msvc",
+
+    # GNU-LLVM Windows targets
+    "x86_64-pc-windows-gnullvm": "x86_64-pc-windows-gnullvm",
+    "aarch64-pc-windows-gnullvm": "aarch64-pc-windows-gnullvm",
 }
 
 _SUPPORTED_CPU_VALUES = {
@@ -48,6 +52,7 @@ def _cpu_from_triple(triple):
 def _os_from_triple(triple):
     if "windows" in triple:
         return "windows"
+    return "unknown"
 
 def _toolchain_name(triple):
     """Generate a Rust toolchain name for the given triple."""
