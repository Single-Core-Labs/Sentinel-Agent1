[package]
name = "sentinel-ai-pager-bin"
version = "1.0.0"
edition.workspace = true
license = "Apache-2.0"
authors = ["AI"]
default-run = "sentinel-ai-pager"

# Composition-root binary for the Ai Build TUI. The artifact is still named
# `sentinel-ai-pager`. This package exists so the binary can link both the pager
# library and the optional `sentinel-ai-pager-minimal` render mode: `minimal`
# depends on `sentinel-ai-pager`, so the pager library cannot depend back on it
# (cargo cycle). The binary installs the minimal-mode IoC hooks at startup.
[[bin]]
name = "sentinel-ai-pager"
path = "src/main.rs"

[dependencies]
# The pager library (all application logic + the public API main.rs drives).
sentinel-ai-pager = { path = "../sentinel-ai-pager" }
# Optional scrollback-native render mode, wired in via the fn-pointer seam.
sentinel-ai-pager-minimal = { path = "../sentinel-ai-pager-minimal" }

# Agent runtime + leader/stdio/headless entry points.
sentinel-ai-shell = { workspace = true }
sentinel-ai-update = { path = "../sentinel-ai-update" }
sentinel-ai-version = { workspace = true }
sentinel-ai-telemetry = { workspace = true }
sentinel-ai-workspace = { workspace = true }
sentinel-crash-handler = { path = "../sentinel-crash-handler" }
sentinel-acp-lib = { workspace = true }

# Async runtime + misc.
tokio = { workspace = true, features = [
    "sync",
    "rt",
    "macros",
    "rt-multi-thread",
    "signal",
    "io-util",
    "io-std",
] }
tokio-util = { workspace = true, features = ["compat"] }
anyhow = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
serde_json = { workspace = true }
dunce = { workspace = true }
semver = { workspace = true }
sentinel-tty-utils = { workspace = true }
sentinel-ai-config = { workspace = true }
sentinel-ai-sandbox = { path = "../sentinel-ai-sandbox", default-features = false }
rustls = { version = "0.23", default-features = false, features = [
    "ring",
    "logging",
    "std",
    "tls12",
] }

# Binary hardening: compile-time string + control-flow obfuscation.
obfstr = { workspace = true }
cryptify = { workspace = true }

[target.'cfg(unix)'.dependencies]
libc = { workspace = true }
# Default allocator on Unix; gated by the `jemalloc` feature (see main.rs
# `#[global_allocator]`, cfg(all(feature = "jemalloc", unix))).
# `stats` is binary-scoped only (not workspace-wide) so this CLI jemalloc
# link gets --enable-stats for stats.allocated / stats.resident mallctl.
tikv-jemallocator = { workspace = true, optional = true, features = ["stats"] }
# Raw mallctl for the memory-cliff arena purge hook (see
# `purge_jemalloc_retained_pages` + the `install_release_hook` call in
# main.rs). Same version family as the
# allocator; adds no new build units beyond what tikv-jemallocator pulls in.
tikv-jemalloc-sys = { workspace = true, optional = true }
# Heap-profile / stats mallctl helpers (prof.active, prof.dump, epoch, stats.*).
tikv-jemalloc-ctl = { workspace = true, optional = true, features = ["stats", "use_std"] }

[dev-dependencies]
serial_test = { workspace = true }
# `PagerArgs::try_parse_from` in the CLI-parsing unit tests (needs the
# `clap::Parser` trait in scope).
clap = { workspace = true, features = ["derive"] }

[features]
default = [
    "jemalloc",
    "sandbox-enforce",
    ]
default-bazel = [
    "jemalloc",
    "sandbox-enforce",
    "local-workspace",
    ]
jemalloc = ["dep:tikv-jemallocator", "dep:tikv-jemalloc-sys", "dep:tikv-jemalloc-ctl"]
sandbox-enforce = ["sentinel-ai-pager/sandbox-enforce"]
local-workspace = ["sentinel-ai-pager/local-workspace"]
release-dist = ["sentinel-ai-pager/release-dist"]
