# Session Log — 2026-07-31

Feature work on Sentinel: **plugin packaging system** + **external policy gate** (`--hook-command`), plus completion of prior-session items (TS TUI default, model selection wiring, BashTool removal, tests for zero-coverage crates, docs/testing-guide merge).

## What was completed today

### 1. External policy gate — `sentinel ai --hook-command <script>`
- `crates/core/sentinel-core/src/event_bus.rs`: new `ScriptPolicyEngine` implementing `PolicyEngine` (fail-closed).
- **Contract (changed during session):** tool name + args JSON are piped to the script's **stdin** (line 1 = tool name, line 2 = args JSON). Script prints one verdict line on stdout:
  - `allow` → Allow
  - `deny <reason>` → Deny(reason)
  - `ask` / `prompt` → PromptUser
  - anything else / error / timeout → **Deny** (fail-closed), 15s default timeout.
- Invocation: `cmd /C <command>` on Windows, `sh -c <command>` on Unix (uses `tokio::process::Command`, NOT blocking `std::process`).
- 5 unit tests (allow / deny+reason / ask+fail-closed / garbage+fail-closed / missing command+fail-closed).

### 2. Plugin system — `sentinel plugin install|list|remove`
- `crates/tools-and-exec/sentinel-plugin-system/src/script.rs` (new crate module):
  - `PluginFileManifest` — parses `sentinel-plugin.toml` (id, name, version, description, optional author/homepage, `[hooks]` table with 6 event keys).
  - `ScriptHook` — script invoked via `cmd <event> <tool_name>` with full event JSON on stdin; stdout `veto <reason>` → Veto, anything else → Continue; 15s timeout; `tokio::process::Command`.
  - `ScriptPlugin`, `load_plugin_dir()`, `load_plugins_dir()`; hook storage uses `std::sync::Mutex<Vec<Box<dyn PluginHook>>>` (drain pattern in `hooks()`).
- `crates/interfaces/sentinel-cli/src/plugin_cmd.rs` (registered as `plugin` command in `main.rs`):
  - `install <dir>` — validates manifest, refuses duplicates, recursive copy into `$SENTINEL_HOME/plugins` else `%USERPROFILE%/.sentinel/plugins`.
  - `list`, `remove`/`uninstall`.
- `sentinel-cli/Cargo.toml` gained `sentinel-plugin-system` dependency.

### 3. Agent wiring for both features
- `crates/core/sentinel-core/src/agent.rs`:
  - `run_with_approval(thread, input, approval, policy: &Option<Arc<dyn PolicyEngine>>)` (new 4th param; `run()` passes `&None`).
  - `execute_tools_concurrent(...)` gained 12th param `plugins: &Arc<PluginRegistry>`.
  - `BeforeToolCall` dispatch → `PluginAction::Veto(reason)` → synthetic error result "Vetoed by plugin policy: …", tool skipped.
  - `AfterToolCall` dispatch in spawned task (`args` cloned before move into `tools.execute`).
  - `dispatch_plugin_event` now returns `PluginAction`.
- `crates/core/sentinel-core/src/pipeline.rs:323` — call site passes `&self.agent.plugin_registry`.
- `crates/core/sentinel-core/src/lib.rs` — added `pub use event_bus::*;` (exports `PolicyEngine`/`ScriptPolicyEngine`).
- Call sites updated: `ai.rs`, `local.rs` (`&None`), `research_tool.rs` (3 sites), `sub_agent.rs`, `sentinel-ai-tui/src/app_server_session.rs`.

### 4. CLI wiring
- `crates/interfaces/sentinel-cli/src/ai.rs`:
  - Parses `--hook-command <c>` (+ help text), builds `Arc<ScriptPolicyEngine>`, prints `⚖ Policy script active: ...`.
  - Loads installed plugins via `load_plugins_dir(plugin_dir())`, registers on agent via `with_plugin_registry`.
  - `plugin_dir()` helper (mirrors `session_dir()`).

## Verification status

| Check | Result |
|---|---|
| `cargo check --workspace` | ✅ clean (0 errors) |
| `cargo test -p sentinel-core --lib event_bus` | ✅ 8 passed |
| `cargo test -p sentinel-plugin-system` | ✅ 8 passed |
| `cargo build -p sentinel-cli` | ✅ Finished (30s) |
| Live: `sentinel plugin install/list/remove` | ✅ works (smoke guard plugin, SENTINEL_HOME=temp) |
| Live: `sentinel ai --hook-command policy.cmd ...` | ✅ policy script active; denied the model's `web_search` call |
| Live: plugin veto path | ⚠️ not exercised live (covered by unit tests `script_hook_vetoes`) |
| `cargo test --workspace` (full suite) | ❌ **NOT RUN** — user stopped it (no time) |

## Remaining / next steps (do these when back)

1. **Run the full suite** (≈ a few minutes):
   - `cargo test --workspace` — expect green except possibly pre-existing failures; report any.
   - `cargo check --workspace` (already clean, re-confirm).
2. **Optional live veto check** (5 min): prompt the model to do something "write"-ish while the smoke guard plugin is installed, grep output for `Vetoed by plugin policy`.
3. **Commit everything** — current uncommitted state is the whole feature build. Suggested message: `feat: scriptable policy gate (--hook-command) + packaged plugin system`. Inspect with `git status`, `git diff --stat` first. (Never commit without explicit ask — user will confirm.)
4. **Optional follow-ups** (not requested, do NOT start without asking):
   - Tests for remaining zero-coverage crates: `sentinel-ide-companion`, `sentinel-ai-test-support`, `sentinel-sdk`, `sentinel-app-server-client`, `sentinel-app-server-daemon`.
   - Cline-SDK-style extras deliberately skipped: cron schedules, inbound connectors, agent teams.

## Key gotchas learned this session (important for resume)

- **Ollama tag:** only `qwen3:latest` + `mistral:7b-instruct-v0.2-q5_0` exist; `qwen3:8b` → 404. Use `qwen3:latest`.
- **Windows cmd + args:** don't append tool_name/args as argv to `cmd /C echo ...` — echo prints everything. Policy contract uses **stdin** for data; hook contract uses argv (event + tool name) but reads JSON from stdin.
- **`tokio::time::timeout` on blocking `std::process::Command::output()` does NOT work** (E0277) — must use `tokio::process::Command` + `spawn()`/`wait_with_output()`. Fixed in both `event_bus.rs` and `script.rs`.
- **UTF-8 BOM breaks .cmd files** — write hook/policy scripts with `Set-Content` (no `-Encoding UTF8`), else `'﻿@echo' is not recognized`.
- **`--force` doesn't exist on `plugin install`** — use `plugin remove <id>` then install.
- **TS TUI needs** `bun run --jsx-import-source=@opentui/solid packages/cli-agent/src/index.tsx` (bun ignores tsconfig `jsxImportSource`).
- **`SENTINEL_NON_INTERACTIVE=1`** + `--prompt` runs headless (no TUI, no WS server) — used for all smoke tests.
- **`SENTINEL_HOME`** overrides plugin/session dirs; session test dir was `%TEMP%\sentinel-smoke\home`.
- Smoke artifacts (guard plugin, policy.cmd) live in `%TEMP%\sentinel-smoke\` — delete freely.

## Files touched today (all new or modified)

- `crates/core/sentinel-core/src/event_bus.rs` (ScriptPolicyEngine + tests)
- `crates/core/sentinel-core/src/agent.rs` (policy param, plugin dispatch, args clone)
- `crates/core/sentinel-core/src/pipeline.rs` (12th arg)
- `crates/core/sentinel-core/src/lib.rs` (export)
- `crates/tools-and-exec/sentinel-plugin-system/src/script.rs` (NEW — manifest/hooks/loader + tests)
- `crates/tools-and-exec/sentinel-plugin-system/Cargo.toml` (toml dep), `lib.rs` (mod script)
- `crates/interfaces/sentinel-cli/src/plugin_cmd.rs` (NEW), `main.rs` (register), `Cargo.toml` (dep), `ai.rs` (--hook-command + plugin loading)
- `crates/interfaces/sentinel-cli/src/local.rs`, `research_tool.rs` (3 sites), `sub_agent.rs`
- `crates/sentinel-ai-tui/src/app_server_session.rs` (per earlier session)
